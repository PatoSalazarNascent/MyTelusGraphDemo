public enum GraphType {
    case lineGraph
    case barGraph
}

public enum AxisType {
    case vertical
    case horizontal
}

public enum GridType {
    case horizontal
    case vertical
    case fullGrid
    case none
}

public enum BarGraphAnimationType {
    case sequence
    case sameTime
    case none
}
