internal enum GraphType {
    case lineGraph
    case barGraph
}

internal enum AxisType {
    case vertical
    case horizontal
}

internal enum GridType {
    case horizontal
    case vertical
    case fullGrid
    case none
}
